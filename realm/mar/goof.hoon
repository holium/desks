|_  guf=goof
++  grow
  |%
  ++  noun  guf
  ++  json
    =/  res=(each (list ^json) tang)
      (mule |.((turn tang.guf tank:enjs:format)))
    ?-    -.res
      %&  o/(malt `(list [@t ^json])`[mote+s/mote.guf tang+a/p.res ~])
      %|  a+[a+[%s '[[output rendering error]]']~]~
    ==
  --
++  grab
  |%
  ++  noun  goof
  --
++  grad  %noun
--
